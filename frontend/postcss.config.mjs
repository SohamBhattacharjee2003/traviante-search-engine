// Tailwind CSS v4 — Next.js uses the PostCSS plugin.
// (The docs' @tailwindcss/vite plugin is for Vite projects; Next.js compiles
//  CSS through PostCSS, so the equivalent v4 plugin is @tailwindcss/postcss.)
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
