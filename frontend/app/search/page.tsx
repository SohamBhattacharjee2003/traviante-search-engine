import ChatExperience from "@/app/components/ChatExperience";

// The destination-discovery experience is now a conversation with Aria, the
// Traviante AI concierge — text + photo turns, with destinations surfaced inline.
export default function ConciergePage() {
  return <ChatExperience />;
}
